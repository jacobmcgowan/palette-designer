import { Color } from '@angular-material-components/color-picker';

export interface IGeneralForm {
  background: Color;
  text: Color;
  surface: Color;
  primary: Color;
  primaryText: Color;
  secondary: Color;
  secondaryText: Color;
}
